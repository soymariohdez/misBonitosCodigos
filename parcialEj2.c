#include <stdio.h>
//Ejercicio 2: Matriz amiga.
int main(){
	int tam, fila, col, sumaFila, sumaAmiga;
	//Definir el tamaño de la matriz.
	printf("¿De qué tamanio desea la matriz?");
	scanf("%d", %tam);
	int matriz[tam][tam];
	for(fila=0; fila<tam: fila++){
		sumaFila=0;
		while(amiga==0){
			for(col=0; col<tam; col++){
			sumaFila=sumaFila+matriz[fila][col];
			}
		}
		
	}
}
